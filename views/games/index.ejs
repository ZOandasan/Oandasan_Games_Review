<%- include('../partials/header') %>

<table>
    <thead>
        <tr>
            <th>Title  </th>
            <th>|</th>
            <th>Genre  |</th>
            <th>Release Year  |</th>
            <th>Xbox Compatable  |</th>
            <th>Playstation Compatable  |</th>
            <th>PC Compatable</th>
        </tr>
    </thead>

    <tbody>
        <% games.forEach(function(g) { %>
        <tr>
            <td><a href="/games/<%= g._id %>"><%= g.title %></a></td>
            <td></td>
            <td><%= g.genre %></td>
            <td><%= g.releaseYear %></td>
            <td>
                <% if (g.xboxCompatability) { %>
                    Yes
                <%} else {%>
                    No
                <% } %>    
            </td>
            <td>
                <% if (g.psCompatability) { %>
                    Yes
                <%} else {%>
                    No
                <% } %> 
            </td>
            <td>
                <% if (g.pcCompatability) { %>
                    Yes
                <%} else {%>
                    No
                <% } %> 
            </td>
        </tr>
        <% }); %>
    </tbody>
</table>

<%- include('../partials/footer') %>